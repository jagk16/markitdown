# Obligatorio: token de Vercel Blob (Dashboard > Storage > Blob > .env.local)
BLOB_READ_WRITE_TOKEN=

# Límite de tamaño por archivo (MB)
MAX_FILE_SIZE_MB=25

# Opcional: Azure Document Intelligence para PDFs escaneados
# AZURE_DOC_INTEL_ENDPOINT=
