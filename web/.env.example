# Creadas automáticamente al conectar Storage → Blob "markitdown"
BLOB_STORE_ID=
BLOB_READ_WRITE_TOKEN=

# Límite servidor (upload + convert.py)
MAX_FILE_SIZE_MB=70
# Debe coincidir con MAX_FILE_SIZE_MB; evita error React #418 en la UI
NEXT_PUBLIC_MAX_FILE_SIZE_MB=70
